<div class="container">
	<a href="/add"></a><button type="button" class="btn btn-success btn-lg" data-toggle="modal" data-target="#userModal" id="user-add">Sign up here!</button>
</div>

<div class="modal fade" id="userModal" tabindex="-1" role="dialog" aria-labelledby="modalLongTitle" aria-hidden="true">
	<div class="modal-dialog" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="modalTitle">Enter your information</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<span aria-hidden="true">&times;</span>
				</button>
      		</div>
			<div class="modal-body">
				<form>
					<div class="form-group">
						<label for="name">Full Name</label>
						<input type="name" class="form-control" id="inputName" placeholder="Enter name">
					</div>
					<div class="form-group">
						<label for="name">Nickname</label>
						<input type="name" class="form-control" id="inputnickname" placeholder="Enter nickname for display on the queue">
					</div>						
					<div class="form-group">
						<label for="email">Email address</label>
						<input type="email" class="form-control" id="inputEmail" aria-describedby="emailHelp" placeholder="Enter email">
					</div>
					<div class="form-group">
						<label for="phone">Phone Number</label>
						<input type="phone" class="form-control" id="inputPhone" placeholder="Phone Number">
					</div>
					<div class="form-group">
						<label for="song">Song Request</label>
						<input type="song" class="form-control" id="inputSong" placeholder="Song Request">
					</div>
					<button type="submit" class="btn btn-primary sub-btn">Submit</button>
				</form>
			</div>
		</div>
	</div>
</div>

{{!-- <div class="song-queue">
	<ul>
		<li>
			{{#each songs}}
				<p>{{this.song song_played=false}}</p>
			{{/each}}
		</li>
	</ul>
</div> --}}
<script type="text/javascript">
var name, nickname, email, phone, newUser;
	$('.sub-btn').on('click', function(event){
		event.preventDefault();
			console.log($('#inputName').val());
			console.log($('#inputnickname').val());
			console.log($('#inputEmail').val());
			console.log($('#inputPhone').val());
				nameVal= $('#inputName').val();
				nicknameVal= $('#inputnickname').val();
				emailVal= $('#inputEmail').val();
				phoneVal= $('#inputPhone').val();
				newUser= {
					name: nameVal,
					nickname: nicknameVal,
					email: emailVal,
					phone: phoneVal,
			}
})
</script>